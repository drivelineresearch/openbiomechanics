# OpenBiomechanics — computer_vision requirements
# Minimum compatible versions (>=); this is not a fully locked environment.
opencv-python>=4.8.0
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=2.5.2
matplotlib>=3.7.0

# NOTE: the hello_world/face_pose demos use a separate, optional legacy stack in
# requirements-face-recognition.txt. It is unmaintained and dlib-based, and can
# need a C++/CMake toolchain. Prefer MediaPipe for new work.
